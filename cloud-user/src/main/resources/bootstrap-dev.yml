server:
  tomcat:
    threads:
      #最小线程数
      min-spare: 10
      #最大线程数
      max: 20
    #最大连接数
    max-connections: 10
    #最大等待数
    accept-count: 10
spring:
  cloud:
    nacos:
      discovery:
        namespace: mycloud-ID
      config:
        namespace: mycloud-ID
##      concurrency: 16